# Контекст проекта ProductRadar

Файл для восстановления контекста в длинных диалогах. Только добавление, не редактирование.

---

## 2026-03-14: API отдельной вакансии — результаты исследования

Все 6 endpoint-ов работают из Google Colab (и GitHub Actions). Ozon НЕ блокирует облачные IP (ранее предполагали что блокирует — опровергнуто).

| Компания | Endpoint | Grade | Description |
|---|---|---|---|
| Yandex | /api/publications/{id} | vacancy.pro_level_max_display → "publications.pro_level.senior" | duties, key_qualifications |
| Wildberries | /crm-api/api/v1/pub/vacancies/{id} (+ Referer) | data.skill_level_id (был null в тесте) | data.description, duties_arr, requirements_arr |
| Ozon | /vacancy/{hhId} | нет | descr (HTML) |
| T-Bank | hrsites-api-vacancies.tbank.ru/.../getVacancy?urlSlug={slug} | experiences[0].name → "Senior" | tasks, requirements (HTML) |
| Sber | .../publications?requisitionId={id} | нет | уже есть в списке |
| Alfa-Bank | /api/vacancies/{id} | нет | уже есть в списке |
| VK | нет API отдельной вакансии | — | — |
| Avito | нет API отдельной вакансии | — | — |

---

## 2026-03-14: Маппинг experience → grade (normalizer)

| Нормализованный опыт | Grade |
|---|---|
| без опыта | Junior |
| до 1 года | Junior |
| 1–3 года | Middle |
| 3–5 лет | Middle+ |
| 5+ лет | Senior |
| lead/head/руководитель/директор/cpo в заголовке | Lead+ |
| не указан | null |

Regex-fallback: извлечь число N из строки, N<1→Junior, 1≤N<3→Middle, 3≤N<5→Middle+, N≥5→Senior.

---

## 2026-03-14: PR-план

- PR 1 — Normalizer + Yandex fix + Telegram output ✅ смержен
- PR 1.1 — Bugfix normalizer edge cases ✅ смержен
- PR 2 — Enrichment API (Yandex, WB, Ozon, T-Bank) ⏳ в работе
- PR 3 — HTML-enrichment (VK, Avito) — только description
- PR 4 — Стоп-слова (фильтрация нерелевантных вакансий)
- PR 5 — Онбординг + фильтры
- PR 6+ — Дайджест, UX, AI-функции

---

## 2026-03-14: Ключевые решения

- grade_guesser удалён, заменён на normalizer — НЕ угадываем grade из заголовка (кроме Lead+)
- grade берётся: 1) из API (прямое поле), 2) Lead+ из заголовка, 3) маппинг experience → grade
- Enrichment = метод enrich() в каждом парсере, не отдельный слой файлов
- Одна компания = один файл парсера (parse + enrich)
- VK и Avito — только HTML, API отдельной вакансии нет (проверено агентом с браузером)
- SHOW_DESCRIPTION = False (описания пока не выводятся в Telegram)
- TEST_MODE = True, TEST_LIMIT = 3 (24 вакансии в базе)

---

## 2026-03-14: Известные особенности данных

- Yandex: pro_levels в API списка — фильтр кандидата, НЕ грейд вакансии. Одна вакансия возвращается на всех уровнях.
- Yandex: experience и grade отсутствуют в API списка. Grade только через enrichment (pro_level_max_display).

---

## 2026-03-14: Yandex pro_level — min, не max

vacancy.pro_level_min_display = минимальный уровень кандидата = грейд позиции.
vacancy.pro_level_max_display = максимальный допустимый уровень = бесполезен (почти всегда senior).
Примеры:
- "Младший менеджер": min=junior, max=senior → grade = Junior
- "Менеджер продукта в KIT": min=middle, max=senior → grade = Middle
Enricher должен брать min, не max.
- Yandex вакансия "Плюс AdTech": город = "Не указан" — это реальное отсутствие данных, не баг.
- WB: skill_level_id был null в тестовой вакансии — может быть заполнен у других.
- Avito: дефолт grade "Middle" убран, теперь null если нет совпадения (Lead/Senior/Junior из заголовка).
- Sber/Alfa experience маппинги через config.py (UUID → строка).

---

## 2026-03-14: PR 5a — per-user доставка, фильтры, Telegram API helper

- PR 5a смержен: per-user доставка, фильтры, `telegram_api` helper.
- Модель доставки: `user_vacancy_delivery` вместо `notified_at`.
- Структура filters: `{"grades": [], "cities": [], "work_formats": [], "companies": []}`.
- Логика фильтрации: пустой список = всё, grade-диапазоны по левой части, `Middle+` = `Middle`, `null`/`Не указан` пропускается.

## PR 5b: Онбординг UI

- Создан `bot/onboarding.py` — state machine онбординга.
- CJM: /start → welcome → [Быстрый старт | Настроить] → grade → city → work_format → company → confirm.
- Toggle без БД: промежуточный выбор живёт в reply_markup, запись в Supabase только при ob:next, ob:done, ob:quick.
- Компании: по умолчанию все включены (🟢). Пустой companies=[] значит "все". В filters хранится список включённых name компании.
- callback_data формат: ob:quick, ob:setup, ob:g:Junior, ob:c:СПб, ob:wf:remote, ob:co:yandex, ob:back, ob:next, ob:done, ob:restart.
- Лоадеры через edit_message на шагах с запросами к БД.
- Fallback при невосстановимом состоянии: "Сессия настройки устарела..."
- Повторный /start сбрасывает filters и перезапускает онбординг.
- Добавлен метод get_user(chat_id) в SupabaseService.
- webhook.py: callback_query и message — взаимоисключающие пути.
- UX-доработка: тексты переписаны от лица бота, добавлен грейд Lead+, «Другой» переименован в «Другой / зарубежные», добавлены пояснения на каждом шаге.

## Hotfix: русские названия компаний + грейды

- Парсеры sber, alfa, tbank, yandex: company переведён на русский (Сбер, Альфа-Банк, Т-Банк, Яндекс). Совпадает с таблицей companies.
- parsers/tbank.py: tags "Head" и "CPO" маппятся в Lead+.
- parsers/avito.py: грейд определяется по префиксу заголовка (специфика Avito, не универсальная эвристика). «Ведущий» → Senior, «Руководитель»/CPO/Head of → Lead+.
- Это не противоречит BUG-009 (запрет на универсальное угадывание грейда из заголовка). Маппинг применяется только к Avito, где API не содержит данных о грейде.

## Запланировано: предупреждение о строгих фильтрах (не реализовано)

На шаге confirm онбординга (или отдельным шагом перед ним) показать предупреждение, если строгие фильтры отсекают значительную часть вакансий из-за пустых полей (grade=null, city=null и т.д.). Пример: «По твоим фильтрам N вакансий. Ещё M вакансий не имеют указанного грейда — показывать их тоже?» с кнопками Да/Нет. Зависит от PR 5c (нужна функция подсчёта вакансий по фильтрам). Реализация — отдельный PR после 5c.


## Hotfix: нормализация work_format, grade, фильтрация городов

- normalize_work_format: маппинг произвольных форматов в Офис/Удалёнка/Гибрид. Множественные значения через запятую разбиваются и нормализуются отдельно.
- normalize_grade: title case, запятая → дефис, Lead/Head/CPO → Lead+.
- filters.py: фильтрация city, work_format, grade устойчива к множественным значениям через запятую. «Любой город» пропускается фильтром.
- Нормализация вызывается в main.py после enrichment, перед записью в БД.

## 2026-03-15: PR 5c — настройки, статистика, порционная выдача

- Порционная выдача: после завершения онбординга в webhook отправляются первые 5 подходящих вакансий, в cron (`main.py`) — максимум 10 вакансий на пользователя за запуск. Кнопка «Ещё» перенесена в PR 5d.
- Добавлен `/settings`: inline-меню с callback prefix `st:`, переиспользует onboarding UI через параметризованный `prefix` в `get_step_message`, `toggle_selection`, `parse_selections_from_markup`.
- В `/settings` доступны пауза/возобновление и отписка. Текстовых команд `/pause` и `/resume` нет.
- Добавлен `/stats`: текстовая статистика за `VACANCY_TTL_DAYS` + количество вакансий, подходящих под фильтры пользователя.
- Исправлен баг фильтра компаний: `filters.companies` хранит `name` компании (русское имя), а не `parser_name`.
- BotFather-меню: команды `/settings` и `/stats`.

## Hotfix: навигация онбординга + закрытие settings

- Кнопка «Не важно» удалена: вместо неё адаптивная кнопка «Пропустить ⏭»/«Далее ➡️» (в зависимости от выбранных значений на шаге).
- На шагах city, work_format и company добавлена кнопка «◀️ Назад» для возврата к предыдущему шагу.
- В `/settings` добавлена кнопка «✕ Закрыть», которая закрывает меню и оставляет текст «Настройки сохранены.».

## 2026-03-15: Hotfix — 5 багов онбординга и фильтрации

- Toggle на шагах grade/city/work_format в онбординге и settings теперь пересчитывает адаптивную кнопку на основании текущих selection из reply_markup, а не из пустых filters.
- Toggle компаний (ob:co:/st:co:) больше не делает повторный запрос `get_enabled_companies()` — устранена гонка при быстрых нажатиях и «карусель» состояния.
- На шаге confirm в онбординге добавлена кнопка «◀️ Назад» рядом с «👍 Отлично» и «🔄 Заново».
- Фильтр grade: сохранён `+` в базовом грейде (`Lead+` матчит `Lead+`), а пользовательские фильтры расширяются (`Middle` → `Middle`+`Middle+`, `Senior` → `Senior`+`Senior+`, `Junior` → `Junior`+`Junior+`).
- Для шага company при toggle обновляется только `reply_markup`; текст сообщения переиспользуется текущий, без пересборки шага и без потери списка компаний.

## Hotfix: рендер компаний, адаптивная кнопка, навигация назад, сводка settings

- BUG-027: `get_step_message("company")` сравнивал `parser_name` с фильтрами, содержащими русские `name`. Исправлено на сравнение по `name`.
- BUG-028: toggle в handlers.py отправлял `reply_markup` из `toggle_selection` без обновлённой навигации. Исправлено: используется полный `reply_markup` из `get_step_message` / `get_settings_step`.
- BUG-029: `ob:back` вызывал `get_step_message` с пустыми фильтрами и без `companies_list`. Исправлено: передаются фильтры из БД и `companies_list` для шага company.
- UX: в `get_settings_menu` добавлена сводка текущих фильтров.
- Confirm-шаг параметризован по prefix.

## 2026-03-15: BUG-030 и UX порционной выдачи

- BUG-030: `_send_onboarding_batch` использовал `limit=6` до фильтрации, из-за чего при узких фильтрах (например, Lead+) подходящие вакансии могли не попасть в выборку.
- Исправлено: сначала загружается полная выборка undelivered (`limit=500`), затем применяется фильтрация, затем `shuffle`, после этого отправляются первые 10.
- UX: в сообщении выдачи показывается общее число найденных вакансий и размер текущей порции.
- UX: после сохранения фильтров в settings показывается кнопка «📬 Получить вакансии» для немедленной выдачи по актуальным фильтрам.
- UX: на confirm-шаге онбординга порядок кнопок слева направо: «🔄 Заново», «◀️ Назад», «👍 Отлично».
- Оптимизация: toggle компаний в settings больше не ходит в БД на каждый клик; обновляется только текущий `reply_markup`.

## 2026-03-15: BUG-031, BUG-032 и UX порционной выдачи

- BUG-031: в `api/webhook.py` глобальный `except Exception` проглатывал ошибки без логов. Добавлено `logging.exception("Webhook error")` и диагностический лог входящего update.
- BUG-032: `_send_onboarding_batch` мог падать после показа лоадера, оставляя пользователя с зависшим «⏳». Добавлен `try/except` с fallback-сообщением: «Произошла ошибка при загрузке вакансий. Попробуй позже или нажми /settings».
- UX: выдача вакансий стала порционной через callback prefix `more:`. Сначала показывается промежуточный экран с количеством найденных вакансий и кнопками «📬 Показать / ⚙️ Изменить фильтры», затем порции по 10 с «📬 Ещё 10 / ✕ Хватит».
- UX: меню `/settings` упрощено — кнопки выбора фильтров идут по 2 в ряд, «✕ Закрыть» заменена на «◀️ Назад», кнопка «📬 Получить вакансии» (после сохранения) вынесена в самый верх.
- UX: confirm-шаг онбординга использует порядок действий слева направо: «🔄 Заново | ◀️ Назад | 👍 Отлично».

## 2026-03-15: BUG-033 — not_in не существует в supabase-py

- В `database/supabase_client.py:get_undelivered_vacancies` использовался несуществующий метод `.not_in()`.
- supabase-py / postgrest-py предоставляет `.not_.in_()` или `.filter("col", "not.in", "(...)")`.
- Баг блокировал ВСЮ повторную выдачу вакансий: при наличии хотя бы одной записи в user_vacancy_delivery любой вызов get_undelivered_vacancies падал с AttributeError.
- Первая выдача при онбординге работала, потому что delivered_ids был пуст и ветка not_in не вызывалась.
- Исправлено вручную: `.not_in()` → `.not_.in_()`.

## 2026-03-15: UX-аудит и технический аудит — результаты

### Проведена виртуальная UX-эмпатия (5 персон, полный CJM)
Ключевые находки:
- UXH-002 (critical): /start сбрасывает фильтры, но не delivery history и не снимает паузу.
- UXH-003 (critical): фильтр "Другой / зарубежные" мёртвый — значение "Другой" не матчит ни один город в базе. Реальные города: Москва (141), Не указан (7), СПб (2), Любой город (2), Минск (1), Алматы/Ташкент (1).
- UXH-004 (major): вакансии с null grade/city/work_format проходят фильтры молча (by design, но пользователю не объясняется).
- UXH-005 (critical): cron в main.py делает limit=10 ДО фильтрации — узкие фильтры теряют релевантные вакансии.
- UXH-006 (major): кнопка "Получить вакансии" в /settings только после st:save.
- UXH-007 (major): "Ещё 10" — не честная пагинация; offset игнорируется, shuffle каждый раз.
- UXH-008 (major): бот не сообщает расписание между рассылками.
- UXH-009 (major): один текст на все zero-state сценарии.
- UXH-014 (critical): /start не снимает paused.
- UXH-015 (major): /stats смешивает рынок (все активные) и личную историю (undelivered).

### Продуктовые решения (приняты 2026-03-15)
1. /start для существующего пользователя: показать "хаб" с вариантами действий (продолжить/перенастроить/полный сброс), а не сбрасывать всё молча.
2. Фильтр города: переосмыслить. Актуальнее формат "Москва / СПб / Удалёнка / Не важно". Проблема: Минск, Алматы и офисные вакансии в других городах выпадают из любой простой схемы. Решение будет прорабатываться отдельно.
3. Null-поля: после настройки фильтров показывать disclaimer, что не все вакансии содержат грейд/город из-за ограничений API компаний. Частично подлечится AI-функциями в будущем.
4. Формат выдачи: поддерживать оба формата — карточки и дайджест. Пользователь выбирает сам (тестовое сообщение при первой настройке).
5. Кнопки на карточках (сохранить/скрыть/похожие): включить в план, реализовывать после стабилизации выдачи.

### Ошибки технического аудитора
- Аудитор утверждал, что delivery/filters.py — заглушка ("просто return vacancies"). Это НЕВЕРНО: файл содержит полноценную реализацию фильтрации по grade/city/work_format/company.
- На этом ложном основании строились выводы W1-01 (реализовать filter-engine), W1-04 (фильтр компаний сломан), и частично UXH-004 (фильтров нет). Реальный статус: фильтры работают, но имеют описанные выше ограничения.
- Вывод: при работе с AI-аудиторами ВСЕГДА перепроверять фактические утверждения о коде через raw GitHub URLs.

### Мёртвый код (подтверждено)
- delivery/telegram.py:deliver_vacancies() — legacy-функция, не используется ни в webhook, ни в main.py. Удалить.

### Раздвоение логики доставки (подтверждено)
- Ручная выдача: bot/handlers.py (_send_vacancies_chunk, _send_onboarding_batch) — limit=500, фильтрация, shuffle, порции по 10.
- Cron-рассылка: main.py:run() — limit=10 ДО фильтрации, без shuffle, отправка всех прошедших фильтр.
- Нужно привести к единой логике: сначала полная выборка, потом фильтрация, потом лимит.

### stop-текст врёт (подтверждено)
- get_stop_confirm() говорит "Фильтры будут сброшены", но st:stop:yes и /stop только деактивируют пользователя, фильтры не трогают. Нужно привести текст к реальности или реально сбрасывать.
