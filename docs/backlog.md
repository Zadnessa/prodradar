# Бэклог ProductRadar

Этот файл — единственный источник правды по задачам проекта. При потере контекста (компрессия чата, новый сеанс) — начинай с чтения этого файла, docs/context.md и AGENTS.md.

Задачи сгруппированы по этапам. Порядок этапов = порядок выполнения. Внутри этапа порядок не важен.

Статусы: [ ] — не начато, [x] — сделано, [~] — частично.

## Этап 1: Cleanup PR (до релиза)

Цель: убрать технический долг, чтобы каждый следующий PR не спотыкался о мёртвый код и дублирование.

- [x] Удалить мёртвый код: deliver_vacancies() и send_telegram_message() в delivery/telegram.py
- [x] Удалить мёртвый код: mark_vacancies_notified() и get_unnotified_vacancies() в database/supabase_client.py
- [x] Удалить мёртвый код: FINAL_TEXT в bot/handlers.py
- [x] Переписать edit_message() в bot/telegram_api.py через _post(), убрать дублирование обработки ошибок
- [x] Удалить salary из парсеров Alfa и Sber (решение: salary не хранится в БД, не извлекается парсерами; рыночная аналитика зарплат — отдельная задача AI-фазы или партнёрства)
- [x] Переименовать short_description в description: колонка в БД + все ссылки в коде (ALTER TABLE vacancies RENAME COLUMN short_description TO description)
- [x] Баг: _send_vacancies_chunk() в bot/handlers.py — проверять результат send_message() до добавления vacancy id в sent_ids (send_message может вернуть None без исключения)
- [x] Webhook secret сделать обязательным: если TELEGRAM_WEBHOOK_SECRET не задан — возвращать 500 и логировать ошибку, не обрабатывать запрос
- [x] Защита webhook от неизвестных chat_id: не создавать SupabaseService и не обрабатывать callback, если chat_id отсутствует в таблице users
- [x] Деактивация вакансий per-company: при ошибке парсера компании X деактивировать только вакансии компаний, чьи парсеры успешно завершились; вакансии компании X не трогать
- [x] Зафиксировать контракт enrich() в AGENTS.md: метод мутирует dict in-place, вызывается до normalizer, НЕ должен перезаписывать поля, которые уже заполнены (кроме short_description, где сравнивается длина)
- [x] Привести docs/context.md в соответствие с текущим кодом (убрать устаревшие пункты из раздела «Принято к исполнению», перенести выполненные в отдельный раздел «Выполнено»)

## Этап 2: UX до релиза (без AI)

Цель: безупречный пользовательский опыт в рамках текущих возможностей без AI. Релиз планируется без AI-функций.

- [ ] Убрать шаг «компании» из онбординга. Онбординг: grade -> city -> work_format -> confirm -> disclaimer. Компании настраиваются только через /settings
- [ ] Добавить колонку category в таблицу companies (значения: e-com, fintech, bigtech, classified, ecosystem и др. — конкретные названия определяет владелец продукта)
- [ ] Двухуровневые настройки в /settings: базовые (грейд, город, формат — 3 кнопки) и расширенные (компании по отраслям). Расширенные: сначала кнопки отраслей (🟢 E-com, 🟢 Финтех), при нажатии на отрасль — список компаний внутри. Blacklist-логика: всё включено по умолчанию, юзер отключает ненужное
- [ ] После завершения онбординга — подсветка: «Хочешь настроить компании? Жми /settings -> Расширенные»
- [ ] Переписать приветственное сообщение бота: акцент на курации компаний с продуктовой культурой и количестве отслеживаемых компаний. Пример: «Мониторим вакансии 25 компаний с сильной продуктовой культурой — от Яндекса до Dodo. Без HH, только первоисточники.»
- [ ] Полная ревизия текстов бота: пройти каждый экран (welcome, шаги онбординга, confirm, disclaimer, hub, /settings basic, /settings advanced, /stop, /stats, zero-state, ошибки). Убрать технические термины (API, webhook, strict_mode). Тон — помощник, не система.
- [ ] Редизайн фильтрации компаний: решить, добавлять ли шаг "выбери индустрии" в онбординг или сделать /settings advanced заметнее после первой выдачи. Взвесить конверсию онбординга vs обнаруживаемость фильтра.

Решения, принятые по UX:
- Пагинация компаний со стрелками — отклонена. Плохой UX.
- Мини-аппы Telegram — отклонены. Дропы онбординга, отдельный хостинг, часть юзеров не откроет.
- Свободный текстовый ввод для фильтров — отклонён. Ломает архитектуру бота (отбивки на нестандартные сообщения, prompt injection).
- Умные пресеты на основе поведения — отложены. Сложно, мало данных на старте.
- Дайджест vs карточки (выбор формата выдачи) — отложен до сбора фидбека после релиза.
- Кнопки на карточках (сохранить, скрыть, похожие) — отложены до сбора фидбека после релиза.

## Этап 3: Масштабирование (критичное — до релиза)

Цель: не упасть при росте данных выше 1000 записей.

- [x] Пагинация Supabase: get_existing_vacancy_hashes() в database/supabase_client.py — лимит 1000 строк на запрос
- [x] Пагинация Supabase: get_active_users() — аналогично
- [x] Пагинация Supabase: get_vacancy_stats() — аналогично
- [x] Пагинация Supabase: deactivate_missing_vacancies() ветка для больших наборов — аналогично
- [x] Пагинация Yandex-парсера: сейчас page_size=100 без цикла, при >100 вакансий потеря данных

## Этап 4: Масштабирование (некритичное — после релиза)

Цель: подготовка к росту до 1000+ пользователей и 50+ компаний.

- [ ] get_undelivered_vacancies() перенести в SQL/RPC с anti-join вместо клиентского NOT IN
- [ ] Фильтрация вакансий на стороне БД вместо паттерна limit -> filter -> slice в Python
- [ ] Telegram API: экспоненциальный backoff на 5xx/429 вместо одной повторной попытки
- [ ] Webhook: разделение transient-ошибок (500, Telegram retry) и бизнес-ошибок (200)
- [ ] Рассылка: sequential delivery по пользователям -> батчи или очередь
- [ ] Rate limiting по chat_id (реализуемо после переезда на VPS, на Vercel serverless in-memory лимит бесполезен)

## Этап 4.5: Аналитика пользовательских действий (до релиза, подготовка)

Цель: подготовить базовую событийную аналитику до релиза без влияния на UX и стабильность бота.

- [ ] Определить список событий для логирования: onboarding_started, onboarding_step_completed, onboarding_finished, vacancies_requested, settings_opened, settings_changed, filter_applied, stop, restart
- [ ] Выбрать хранилище: таблица user_events в Supabase (простейший вариант на старте) или внешний сервис (Amplitude/Mixpanel)
- [ ] Принцип zero-impact: запись событий асинхронная, fire-and-forget; падение записи не блокирует пользователя
- [ ] Дашборд — отложить до VPS (Этап 8). На старте — SQL-запросы вручную.

## Этап 5: Расширение компаний (параллельно с UX, до релиза)

Цель: довести список с 9 до 22+ компаний. 20+ компаний — обязательное условие релиза.

Текущие парсеры (9): Avito, Yandex, T-Bank, Alfa-Bank, Ozon, Sber, VK, Wildberries, Dodo.

Компании для добавления:
- Касперский
- X5 / X5 Tech (проверить: дублируются ли вакансии IT-юрлица и основного бренда — если да, хватит одного парсера)
- Точка Банк
- Aviasales
- МТС
- Контур
- Lamoda
- ВТБ
- hh.ru (работодатель, не источник данных)
- Циан
- Райффайзен
- Wildberries & Russ (проверить: отличается ли от текущего парсера Wildberries)

Экосистемные дочерние бренды (требуют отдельного ресерча):
- Сбер: СберЗдоровье, Звук, СберМаркет, Okko/KION
- Яндекс: Лавка, Путешествия, Плюс
- МТС: KION (если не Сбер), МТС Music и др.
Критерий включения: наличие собственного карьерного сайта с активными продуктовыми вакансиями.

IT-юрлица (Ozon Tech, X5 Tech и т.п.): писать парсер только если вакансии уникальны и не дублируют основной бренд.

API-ресерч:
- [ ] Делегировать ChatGPT батчами по 3 компании
- [ ] Двухшаговый API-ресерч через LLM: промпт 1 — extraction (все эндпоинты, поля, пагинация); промпт 2 — audit (сжатый отчёт, что релевантно для бота, что отбросить). Финальный выход — одна секция в docs/api_spec.md на компанию.
- [ ] Формат выхода: название компании, URL карьерного сайта, тип источника (JSON API / HTML SSR / SPA без API), пример URL вакансии, наличие пагинации, доступные поля (title, city, grade, description, published_at), дочерние бренды с отдельными джоббордами
- [ ] Каждый результат верифицировать вручную (ChatGPT склонен галлюцинировать ссылки и структуры API)
- [ ] Результаты фиксировать в docs/api_spec.md
- [ ] Написание и тестирование парсеров
- [ ] Определить конкретные названия отраслей для category (владелец продукта)

Рекомендуемый порядок батчей:
- Батч 1: Kaspersky, Dodo, X5 Tech (вероятнее всего JSON API).
- Батч 2: MTS, Kontur, Lamoda.
- Батч 3: VTB, Cian, Raiffeisen.
- Батч 4: Aviasales, hh.ru (employer), Точка Банк.
- Батч 5: Экосистемные суббренды (после основных парсеров).


## Этап 6: AI-фаза (после релиза и фидбека)

Цель: уточнение данных вакансий через LLM, определение сегмента, генерация описаний.

Архитектурные решения:
- AI-результаты хранятся в отдельной таблице vacancy_ai_enrichment (vacancy_id FK, ai_grade, ai_segment, ai_card_summary, ai_model, ai_prompt_version, ai_generated_at, ai_status, ai_error). Таблица vacancies не меняется.
- При выдаче вакансии — LEFT JOIN: если AI-запись есть и ai_status=success, берём AI-поля; если нет — берём из vacancies.
- Kill switch: возможность отключить AI целиком (не делать JOIN) или по отдельному полю (игнорировать ai_grade, но брать ai_segment).
- AI-шаг в пайплайне: после enrich() и _prepare_vacancy(), но до финального сохранения. Отдельная функция ai_enrich(vacancy), не внутри парсеров.
- Идемпотентность: хеш AI-input (description + title + grade + company), повторный прогон LLM только при смене входных данных или версии промпта.

Задачи:
- [ ] Создать таблицу vacancy_ai_enrichment
- [ ] Определение сегмента вакансии (B2B/B2C/e-com/fintech/classified) по описанию — ключевая задача, т.к. внутри экосистем (Сбер, Яндекс, МТС) разные команды относятся к разным отраслям
- [ ] Определение сегмента на уровне вакансии (не компании): внутри экосистем одна команда может быть e-com, другая — fintech. Временный маппинг category на уровне companies — костыль до AI-фазы.
- [ ] Уточнение грейда по описанию (few-shot на выборке Senior-вакансий)
- [ ] Генерация краткого описания для карточки (1 абзац)
- [ ] Рыночная аналитика зарплат (партнёрство или сбор данных — отдельное исследование)
- [ ] CJE-вакансии: вернуть в выдачу после AI-классификации описаний (по подстроковому фильтру невозможно — 2 из 5 оказались продуктовыми)
- [ ] Сбер: PM-вакансии без продуктовых слов в заголовке (например, SB019 Лидер направления по AI). Ловятся только через анализ description
- [ ] Dodo: enrichment для description доступен (HTML-блоки vacancy_text/vacancy_expectation/vacancy_you_will/vacancy_benefits), но grade в detail endpoint может быть пустым. Проверить полноту данных при появлении PM-вакансий

## Этап 7: HH.ru и дедупликация (после AI-фазы)

Цель: проверить, есть ли на HH вакансии, которых нет на прямых карьерных сайтах.

- [ ] Парсер HH.ru по employer_id (API: api.hh.ru, у каждой компании свой employer_id)
- [ ] Дедупликация через embeddings + cosine similarity (без AI — ненадёжно, fuzzy match по title+company даёт ложные срабатывания)
- [ ] Принять решение: HH как дополнение к прямым сайтам или как fallback для компаний без карьерного сайта

## Этап 8: Инфраструктура (по мере необходимости)

Цель: подготовка к переезду на VPS, расшивка монолитов.

- [ ] Переезд на VPS с бэкапами (когда Vercel станет узким местом; webhook.py заменяется на FastAPI за 20 минут)
- [ ] ENV-based конфигурация — уже частично реализована, довести до конца
- [ ] Расшивка main.py: сбор, обогащение, доставка — отдельные модули (естественная необходимость при добавлении AI-шага)
- [ ] Расшивка bot/handlers.py: onboarding, settings, delivery — отдельные модули
- [ ] Расшивка SupabaseService на мелкие репозитории по доменам (vacancies, users, delivery)
