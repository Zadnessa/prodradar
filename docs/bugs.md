# Лог исправленных багов

### BUG-001: importlib вместо PARSER_REGISTRY
Файл: main.py
Было: динамический импорт через importlib находил BaseParser и падал.
Стало: используется `from parsers import PARSER_REGISTRY`.
Причина: точка входа обходила реестр парсеров и цепляла лишние классы.

### BUG-002: Markdown вместо HTML в Telegram
Файл: delivery/telegram.py
Было: сообщения отправлялись с `parse_mode="Markdown"`, в тексте появлялись лишние обратные слэши.
Стало: используется `parse_mode="HTML"`, `_escape_html` и HTML-теги.
Причина: формат сообщений не соответствовал выбранному способу рендеринга в Telegram.

### BUG-003: пустые заголовки в Avito
Файл: parsers/avito.py
Было: `card.find("a")` находил пустую ссылку-оверлей, и `title` сохранялся пустым.
Стало: используется селектор `a.vacancies-section__item-name`, заголовки извлекаются корректно.
Причина: парсер брал не тот DOM-элемент из карточки вакансии.

### BUG-004: отсутствующие грейд и опыт в Yandex
Файл: parsers/yandex.py
Было: грейд и опыт не определялись, потому что `pro_levels` не было в объекте вакансии.
Стало: грейд и опыт определялись из заголовка через `guess_grade_from_title`.
Причина: исходная реализация опиралась на поле, которого не было в выдаче списка.

### BUG-005: лишние пробелы в названиях вакансий
Файл: parsers/*.py
Было: `title` мог сохраняться с ведущими и замыкающими пробелами.
Стало: название вакансии нормализуется через `.strip()` перед сохранением.
Причина: парсеры сохраняли сырой текст без базовой очистки.

### BUG-006: затирание short_description в main.py
Файл: main.py
Было: `short_description` всегда перезаписывался результатом `generate_summary`, включая `None`.
Стало: `generate_summary` вызывается только если `short_description` отсутствует.
Причина: логика обогащения не проверяла, заполнено ли поле до генерации summary.

### BUG-007: грейды Yandex через фильтр pro_levels
Файл: parsers/yandex.py
Было: грейд и опыт определялись только из заголовка.
Стало: данные собирались отдельными запросами по `pro_levels` с дедупликацией по приоритету грейда и fallback-запросом без фильтра.
Причина: была временно выбрана неверная стратегия получения грейда из API Яндекса.

### BUG-008: Yandex pro_levels не определяет грейд вакансии
Файл: parsers/yandex.py
Было: выполнялись 4 запроса по `pro_levels` с дедупликацией по приоритету грейда.
Стало: выполняется один запрос без `pro_levels`, а `grade` определяется через normalizer или enrichment API.
Причина: `pro_levels` — это фильтр допустимого уровня кандидата, а не грейд вакансии.

### BUG-009: grade угадывался из заголовка вакансии
Файл: enrichment/grade_guesser.py (удалён)
Было: `guess_grade_from_title` определял Junior/Middle/Senior по ключевым словам в заголовке.
Стало: `grade` определяется только из фактических данных API, маппинга `experience -> grade` или специальных исключений.
Причина: эвристика по заголовку давала недостоверные и неустойчивые результаты.

### BUG-010: Normalizer не покрывает краевые случаи experience
Файл: enrichment/normalizer.py
Было: строки вроде `от 3 лет` и `более 6 лет` не нормализовались.
Стало: добавлен regex-fallback для любых строк с числами.
Причина: normalizer обрабатывал только заранее перечисленные шаблоны опыта.

### BUG-011: Telegram потерял полужирные подзаголовки
Файл: delivery/telegram.py
Было: поля выводились без тегов `<b>`.
Стало: названия полей снова отображаются полужирным, но в нижнем регистре.
Причина: при переходе на HTML-разметку потерялась часть форматирования сообщения.

### BUG-012: Yandex enrichment брал max вместо min pro_level
Файл: parsers/yandex.py
Было: `grade` брался из `pro_level_max_display`, что почти всегда давало Senior.
Стало: используется минимум и максимум, а при различии формируется диапазон `Middle-Senior`.
Причина: был перепутан смысл минимального и максимального уровня кандидата.

### BUG-013: T-Bank терял второй грейд из tags
Файл: parsers/tbank.py
Было: `grade = tags[0]`.
Стало: учитываются все грейд-теги, а при нескольких значениях формируется диапазон.
Причина: парсер брал только первый тег и терял остальную информацию.

### BUG-014: en-dash (–) → дефис (-) в experience и grade
Файл: enrichment/normalizer.py, parsers/yandex.py, parsers/tbank.py
Было: в нормализованных строках и диапазонах использовался en-dash `–`.
Стало: используется обычный дефис `-`, а входные ключи с en-dash сохранены для совместимости.
Причина: разные символы тире ломали единообразие данных и фильтрацию.

### BUG-015: удалено автоматическое определение Lead+ из заголовков
Файл: enrichment/normalizer.py, main.py, parsers/avito.py
Было: Lead+ и часть грейдов определялись эвристиками по заголовку.
Стало: автоматическое определение Lead+ из заголовка удалено; остаются только фактические данные enrichment и `grade_from_experience`.
Причина: общая эвристика давала ложные срабатывания и противоречила новому контракту grade.

### BUG-016: Т-Банк теряет грейд Head из tags
Файл: parsers/tbank.py
Было: `grade_priority` содержал только Junior, Middle, Senior и Lead, поэтому тег `Head` не распознавался.
Стало: добавлены `Head` и `CPO` с маппингом в `Lead+`.
Причина: справочник грейдов не покрывал реальные значения из API Т-Банка.

### BUG-017: Avito — все вакансии без грейда
Файл: parsers/avito.py
Было: `grade` всегда был `null`, потому что API Avito его не отдаёт.
Стало: для Avito введён специальный маппинг по номенклатуре заголовков (`Ведущий` → Senior, `Руководитель` → Lead+).
Причина: у источника нет поля grade, а без исключения все вакансии оставались без уровня.

### BUG-018: VK грейды в нижнем регистре и через запятую
Файл: enrichment/normalizer.py, main.py
Было: `grade` из VK записывался как есть: `middle`, `senior`, `middle, senior`.
Стало: `normalize_grade` приводит значения к Title Case, меняет запятую на дефис и маппит Lead/Head/CPO в `Lead+`.
Причина: значения грейда не приводились к общему формату перед сохранением.

### BUG-019: work_format — 14 вариантов вместо трёх
Файл: enrichment/normalizer.py, main.py
Было: парсеры сохраняли `work_format` как есть из API.
Стало: `normalize_work_format` маппит значения в стандартные `Офис`, `Удалёнка`, `Гибрид`, `Не указан`.
Причина: отсутствие единого словаря ломало фильтрацию и аналитику.

### BUG-020: фильтрация не работала для множественных городов и форматов
Файл: delivery/filters.py
Было: использовалось точное сравнение строк, поэтому `Москва, Санкт-Петербург` не матчилось ни с одним фильтром.
Стало: строка разбивается по запятой, матч идёт по любому фрагменту, а `Любой город` пропускается.
Причина: фильтр не учитывал, что одно поле может содержать несколько значений.

### BUG-021: filters.companies хранил parser_name вместо name
Файл: bot/onboarding.py, bot/handlers.py
Было: в `filters.companies` сохранялся `parser_name`, из-за чего фильтрация по компаниям не совпадала с вакансиями.
Стало: сохраняется `name` компании через маппинг `parser_name -> name`.
Причина: формат значения в фильтре не совпадал с данными вакансий.

### BUG-022: навигация онбординга и закрытие settings
Файл: bot/onboarding.py, bot/handlers.py, bot/settings.py
Было: использовалась кнопка `Не важно →`, нельзя было вернуться назад, а в settings не было явного закрытия меню.
Стало: навигация стала адаптивной (`Пропустить ⏭`/`Далее ➡️`), добавлена кнопка `◀️ Назад`, а в settings появилась кнопка `✕ Закрыть` (`st:close`).
Причина: сценарий настройки был слишком линейным и плохо управлялся из интерфейса.

### BUG-023: адаптивная кнопка не обновлялась при toggle
Файл: bot/handlers.py
Было: при toggle вызывался `get_step_message(step, {})`, поэтому кнопка всегда считала выбор пустым и оставалась `Пропустить ⏭`.
Стало: после toggle выбор читается из `reply_markup` через `parse_selections_from_markup`, и кнопка пересчитывается по фактическому состоянию.
Причина: UI пересобирался не из текущего состояния кнопок, а из пустых фильтров.

### BUG-024: карусель компаний при быстрых нажатиях
Файл: bot/handlers.py
Было: каждый toggle компаний вызывал `db.get_enabled_companies()`, и ответы могли приходить не по порядку.
Стало: в ветках `ob:co:` и `st:co:` повторный запрос убран; обновляется только `markup` текущего сообщения.
Причина: лишние запросы к БД создавали гонку и затирали актуальное состояние интерфейса.

### BUG-025: отсутствовала кнопка «Назад» на confirm
Файл: bot/onboarding.py
Было: на шаге `confirm` были только `Отлично` и `Заново`.
Стало: добавлена кнопка `◀️ Назад` (`ob:back`) в один ряд с остальными действиями.
Причина: у пользователя не было короткого пути вернуться к предыдущему шагу проверки.

### BUG-026: Lead+ не проходил фильтр и не учитывались plus-варианты
Файл: delivery/filters.py
Было: `_grade_base` удалял `+`, поэтому `Lead+` превращался в `lead` и не матчил вакансии `Lead+`.
Стало: `+` сохраняется в `_grade_base`, а пользовательский список грейдов расширяется вариантами `middle+`, `senior+`, `junior+`.
Причина: базовая нормализация грейдов обрезала значимый символ уровня.

### BUG-027: get_step_message("company") сравнивал parser_name с name
Файл: bot/onboarding.py
Было: состояние кнопок компаний вычислялось через `parser_name in enabled_companies`, хотя в `filters.companies` хранятся русские `name`.
Стало: сравнение идёт по `label` (`name`), и кнопки корректно показывают `🟢/🔴`.
Причина: UI использовал другой идентификатор компании, чем фильтр пользователя.

### BUG-028: toggle-ветки не обновляли адаптивную кнопку навигации
Файл: bot/handlers.py
Было: в ветках `ob:g/c/wf` и `st:g/c/wf` в `edit_message` уходил `reply_markup` из `toggle_selection`, где нижний ряд оставался старым.
Стало: после `toggle_selection` и `parse_selections_from_markup` финальный `reply_markup` берётся из `get_step_message` или `get_settings_step`.
Причина: toggle менял только кнопку выбора, но не пересобирал навигационный ряд.

### BUG-029: ob:back терял фильтры и список компаний
Файл: bot/handlers.py
Было: `ob:back` вызывал `get_step_message(previous_step, {})` и не передавал `companies_list` для шага `company`.
Стало: в `get_step_message` передаются `user.get("filters")` и `companies_list` с лоадером `⏳` перед запросом к БД.
Причина: возврат на предыдущий шаг восстанавливал не текущее состояние пользователя, а пустой контекст.

### BUG-030: limit до фильтрации скрывал релевантные вакансии
Файл: bot/handlers.py
Было: `_send_onboarding_batch` брал только `limit=6`, потом фильтровал и отправлял до 5 вакансий.
Стало: метод берёт `limit=500`, сначала фильтрует и перемешивает результаты, затем отправляет до 10 вакансий.
Причина: лимит применялся слишком рано и отрезал релевантные вакансии при узких фильтрах.

### BUG-031: webhook проглатывал ошибки без логирования
Файл: api/webhook.py
Было: `except Exception` возвращал `{ok:false}` без записи стека в логи.
Стало: добавлены `logging.exception("Webhook error")` и диагностический лог входящего `update/callback`.
Причина: глобальный обработчик ошибок скрывал информацию, нужную для диагностики в продакшене.

### BUG-032: зависание лоадера при ошибке выдачи вакансий
Файл: bot/handlers.py
Было: `_send_onboarding_batch` не имел защитного `try/except`, и пользователь мог навсегда остаться с сообщением `⏳`.
Стало: добавлен `try/except` с fallback-сообщением пользователю и логированием через `logging.exception`.
Причина: сценарий с лоадером не был защищён от исключений во время выдачи.

### BUG-033: .not_in() не существует в supabase-py
Файл: database/supabase_client.py
Было: в `get_undelivered_vacancies` использовался несуществующий метод `.not_in()`.
Стало: вызов заменён на `.not_.in_()`.
Причина: реализация опиралась на метод, которого нет в `supabase-py/postgrest-py`.

### BUG-034: названия компаний не совпадали с таблицей companies
Файл: parsers/sber.py, parsers/alfa.py, parsers/tbank.py, parsers/yandex.py
Было: часть парсеров сохраняла названия компаний не в том виде, который используется в таблице `companies`.
Стало: названия приведены к русским значениям, совпадающим со справочником компаний.
Причина: значения в парсерах и справочнике Supabase не были синхронизированы.

### BUG-068: title_confidence всегда NULL в on-demand выдаче
Файл: database/supabase_client.py, bot/handlers.py
Было: get_undelivered_vacancies запрашивал колонку title_confidence, которой нет в таблице vacancies; _vacancy_sort_key получал NULL и сортировка по confidence не работала.
Стало: title_confidence вычисляется on-the-fly через classify_title из delivery/ranking.py; колонка убрана из SELECT.
Причина: при реализации витрины (T-001) функция ранжирования ожидала значение из БД, но запись title_confidence в словарь вакансии при upsert не была добавлена.

### BUG-069: лоадер зависает при ошибке в handle_main_keyboard_text
Файл: bot/handlers.py
Было: handle_main_keyboard_text вызывал _send_onboarding_batch без try/except; при исключении сообщение "Подбираю вакансии..." оставалось навсегда.
Стало: вызов обёрнут в try/except; при ошибке лоадер редактируется в сообщение об ошибке.
Причина: защитная обёртка BUG-032 покрывала внутренность _send_onboarding_batch, но не внешний вызов из handle_main_keyboard_text.

### BUG-070: прямой доступ к db.client.table в _count_delivered_before_request
Файл: bot/handlers.py
Было: функция обращалась к db.client.table("user_vacancy_delivery") напрямую, минуя SupabaseService.
Стало: вызывается db.count_delivered(chat_id), логика инкапсулирована в SupabaseService.
Причина: нарушение архитектурного правила — все запросы к БД через SupabaseService.

## Шаблон для новых записей

### BUG-NNN: краткий заголовок
Файл: путь
Было: описание проблемы
Стало: описание решения
Причина: почему так случилось


### BUG-035: T-Bank filters.tag возвращает 503
Файл: parsers/tbank.py
Было: filters.tag: ["product-manager"] — сервер отвечает 503.
Стало: tcareer_it_profession: ["product-management"] — сервер отвечает 200.
Причина: T-Bank изменил API фильтрации, старый ключ больше не поддерживается.

### BUG-036: T-Bank getVacancy пустышка для publisher
Файл: parsers/tbank.py
Было: enrich() запрашивал getVacancy?urlSlug={slug}, для source=publisher получал пустые поля.
Стало: enrichment отключён, метод возвращает vacancy без HTTP-запроса.
Причина: эндпоинт getVacancy не поддерживает вакансии из внешнего ATS (source=publisher).

### BUG-037: T-Bank cities всегда пустой массив
Файл: parsers/tbank.py
Было: город брался из cities — всегда [''], результат "Не указан".
Стало: город берётся из regionId с маппингом через city_mappings.
Причина: поле cities в API T-Bank не заполняется, реальный город в regionId (FIAS UUID).

### BUG-038: T-Bank URL вакансии нерабочий
Файл: parsers/tbank.py
Было: URL строился как /career/{source}/{specialty}/{urlSlug}/ — ссылка не открывалась.
Стало: URL строится как /career/it/vacancy/{city_slug}/{seoSlug}/{urlSlug}/.
Причина: шаблон URL в спецификации не соответствовал реальной структуре сайта.

### BUG-039: Ozon limit=100 обрезается сервером до 50
Файл: parsers/ozon.py
Было: meta.limit=100, сервер возвращал только 50, пагинация отсутствовала.
Стало: meta.limit=50, добавлена пагинация по meta.page/meta.totalPages.
Причина: серверное ограничение perPage=50 не было учтено.

### BUG-040: Alfa-Bank URL через id вместо slug
Файл: parsers/alfa.py
Было: URL строился как /vacancies/{id} — работает через редирект, но не каноническая ссылка.
Стало: URL строится как /vacancies{slug} (slug содержит ведущий слеш).
Причина: каноническая ссылка использует slug с городом и транслитерированным названием.

### BUG-041: Alfa-Bank work_format всегда "Не указан"
Файл: parsers/alfa.py
Было: work_format не извлекался, всегда "Не указан".
Стало: загружается справочник archetypes через /api/optionLists, archetypeId маппится в название.
Причина: формат работы хранится как archetypeId, расшифровка только через справочник.

### BUG-042: VK limit=100 обрезается сервером до 50
Файл: parsers/vk.py
Было: limit=100, сервер возвращал только 50, часть вакансий терялась при >50 результатах.
Стало: limit=50, добавлена пагинация через поле next/offset.
Причина: серверное ограничение limit=50 не было учтено.

### BUG-043: VK _extract_section_text_from_h3 возвращает None, контент лежит в div.article внутри grandparent
Файл: parsers/vk.py
Было: метод искал `header.parent.next_siblings`, упирался в служебный блок вне основного контента и не находил секции `Задачи` и `Требования`.
Стало: поиск идёт через `grandparent` в `div.article`, а текст собирается от нужного `h3` до следующего `h3` или конца статьи.
Причина: HTML-структура страницы хранит полезный контент не рядом с обёрткой заголовка, а внутри вложенного блока `article`.

### BUG-044: T-Bank enrich не перезаписывает short_description если parse() уже заполнил из API
Файл: parsers/tbank.py
Было: `enrich()` обновлял `short_description` только если поле было пустым, поэтому длинное HTML-описание проигрывало короткому `shortDescription` из API.
Стало: `enrich()` сравнивает длину HTML-summary с текущим `short_description` и заменяет поле только если HTML-версия длиннее.
Причина: контракт «не затирать заполненное поле» оказался слишком жёстким для случая, когда enrichment даёт более полный текст того же поля.

### BUG-045: Ozon workFormat содержит \xa0 (неразрывный пробел), нормализатор не матчит
Файл: parsers/ozon.py
Было: значения `workFormat` приходили с `\xa0`, например `На\xa0месте работодателя`, и не совпадали с ожидаемыми строками нормализатора.
Стало: все строковые элементы `workFormat` очищаются заменой `\xa0` на обычный пробел до дальнейшей обработки.
Причина: API Ozon использует неразрывные пробелы в текстовых значениях формата работы.

### BUG-046: save_vacancies использовал insert без обновления существующих вакансий
Файл: database/supabase_client.py, main.py
Было: `save_vacancies()` делал только `insert`, поэтому изменения вакансии с тем же `id` не попадали в базу.
Стало: запись разбита на `insert/touch/update` по `content_hash`, а `save_vacancies()` удалён.
Причина: пайплайн различал только новые `id` и не отслеживал изменение содержимого вакансии.

### BUG-047: снятые вакансии оставались is_active=true навсегда
Файл: main.py
Было: вакансии, исчезнувшие из API, не деактивировались и продолжали считаться активными.
Стало: после каждого прогона вакансии, отсутствующие в текущей выдаче API, переводятся в `is_active=false`.
Причина: в пайплайне не было шага синхронизации активных вакансий с актуальной выдачей источников.

### BUG-048: VK enrich() перезаписывал work_format regex-мусором из meta description
Файл: parsers/vk.py
Было: `enrich()` извлекал `work_format` regex-ом из `meta description` и затирал корректное значение из API строками вроде `полный`.
Стало: метод `_extract_work_format_from_meta` удалён, а `enrich()` больше не перезаписывает `work_format`.
Причина: свободный текст из meta description не является надёжным источником структурированного поля.

### BUG-049: T-Bank city_slug из UUID не распознавался сайтом, HTML без секций
Файл: parsers/tbank.py
Было: URL вакансии строился с `city_slug`, полученным из `regionId`, и без маппинга туда попадал UUID, из-за чего сайт возвращал HTML без секций `Описание`, `Обязанности`, `Требования`.
Стало: `city_slug` захардкожен как `moscow` для всех вакансий.
Причина: SSR-страница T-Bank требует валидный slug города, а `moscow` стабильно открывает полную HTML-версию для вакансий любого города.

### BUG-050: Сбер "Полный день" не матчился нормализатором work_format
Файл: enrichment/normalizer.py
Было: `normalize_work_format()` не распознавал строки `Полный день`, поэтому данные Сбера нормализовались в `Не указан`.
Стало: добавлена подстрока `полн` в маппинг на `Офис`.
Причина: нормализатор покрывал `офис/на месте/office`, но не учитывал распространённый русский вариант офисного графика.

### BUG-051: short_description обрезался до 500 символов во всех парсерах
Файл: parsers/tbank.py, parsers/ozon.py, parsers/alfa.py, parsers/sber.py, parsers/vk.py, parsers/yandex.py, parsers/wildberries.py, parsers/avito.py
Было: `short_description` сохранялся с обрезкой `[:500]`.
Стало: лимит длины удалён, в поле сохраняется полный текст.
Причина: AI-фазе нужен полный контекст описания вакансии без усечения.

### BUG-052: Avito work_format не использовал fallback на data-vacancy-remote
Файл: parsers/avito.py
Было: при пустом или отсутствующем `span.vacancies-section__item-format` парсер всегда сохранял `work_format="Не указан"`.
Стало: если `span` пустой, парсер использует `data-vacancy-remote`: значение `Да` маппится в сырой `work_format="удаленно"`, `Нет` остаётся `Не указан`.
Причина: карточка Avito содержит отдельный data-атрибут удалённости, который не использовался как fallback.

### BUG-053: experience оставался "не указан" при заполненном grade
Файл: enrichment/normalizer.py, main.py
Было: пайплайн умел выводить `grade` из `experience`, но не делал обратный маппинг `grade -> experience`.
Стало: добавлен `experience_from_grade()`, а в `_prepare_vacancy` опыт выводится из заполненного грейда, если исходный `experience` остался `не указан`.
Причина: источники вроде VK, Avito и T-Bank часто отдают только грейд, поэтому опыт терялся без обратного словаря.

### BUG-054: city оставался "Не указан" для удалённых вакансий без города
Файл: main.py
Было: после нормализации `city` мог оставаться `Не указан`, даже если `work_format` уже нормализован в `Удалёнка`.
Стало: `_prepare_vacancy` подставляет `city="Удалёнка"`, если город не указан, а формат работы содержит `Удалёнка`.
Причина: для полностью удалённых вакансий отсутствие города не несёт смысла и ухудшает фильтрацию/отображение.

### BUG-055: content_hash включал short_description, из-за чего enrich() не вызывался повторно при неизменных данных parse()
Файл: database/supabase_client.py
Было: `content_hash` включал `short_description`, хотя хеш считался до `enrich()`, а enrichment позже менял описание.
Стало: `short_description` исключён из `CONTENT_HASH_FIELDS`, и хеш теперь зависит только от `title`, `grade`, `city`, `work_format`, `experience`.
Причина: описание из enrichment создавало рассинхрон между сохранённым хешем и повторным parse(), из-за чего обновление описания могло зациклиться и больше не запускаться.

### BUG-056: _send_vacancies_chunk не проверял результат send_message
Файл: bot/handlers.py
Было: `vacancy_id` добавлялся в `sent_ids` даже если `send_message()` вернул `None`.
Стало: `vacancy_id` добавляется в `sent_ids` только при успешной отправке (`send_message()` вернул result).
Причина: Telegram API helper может вернуть `None` без исключения (например, 403/429), и такие вакансии нельзя помечать доставленными.

### BUG-057: webhook принимал запросы без secret
Файл: api/webhook.py
Было: если `TELEGRAM_WEBHOOK_SECRET` не задан, проверка секрета фактически отключалась, и webhook принимал запрос.
Стало: при пустом `TELEGRAM_WEBHOOK_SECRET` webhook возвращает HTTP 500 и не обрабатывает update.
Причина: защитная проверка была завязана на условие `if secret and ...`, что оставляло endpoint без обязательной верификации.

### BUG-058: деактивация пропускалась целиком при ошибке одного парсера
Файл: main.py, database/supabase_client.py
Было: при любой ошибке парсера деактивация вакансий отключалась для всех компаний.
Стало: деактивация выполняется только для компаний с успешным парсингом через параметр `companies` в `deactivate_missing_vacancies()`.
Причина: глобальный skip-деактивации создавал вечные активные вакансии у компаний, чьи парсеры отработали успешно.

### BUG-059: "опубликовано" показывало created_at при отсутствии published_at
Файл: delivery/telegram.py
Было: _format_published_at_label использовал fallback vacancy.get("published_at") or vacancy.get("created_at"), из-за чего для VK, T-Bank, Wildberries показывалась дата обнаружения нами как дата публикации.
Стало: используется только vacancy.get("published_at"). Если поле пустое — блок "опубликовано" не отображается.
Причина: created_at — дата появления в нашей БД, не дата публикации работодателем. Показывать её как "опубликовано" вводит пользователя в заблуждение.

### BUG-060: Sber keyword-фильтр отсекал релевантные вакансии
Файл: parsers/sber.py
Было: parse() фильтровал вакансии по ключевым словам (product, продакт, продукт, cpo) в заголовке, из-за чего терялись PM-вакансии с нестандартными названиями (например, "Владелец продукта", "CJE").
Стало: keyword-фильтр удалён, фильтрация выполняется общим TITLE_STOP_PATTERNS + SBER_TITLE_WHITELIST в main.py.
Причина: анализ 104 вакансий Сбера показал 73% мусора; точечный keyword-фильтр в парсере не покрывал все варианты продуктовых заголовков.

### BUG-061: Alfa search=продукт ограничивал выдачу
Файл: parsers/alfa.py
Было: URL запроса содержал параметр search=продукт, из-за чего API не возвращал вакансии без слова "продукт" в полях (например, "AI product manager", "Лидер продуктового стрима").
Стало: параметр search убран, businessLine=1020 достаточно для фильтрации IT-вакансий.
Причина: серверная текстовая фильтрация не покрывала англоязычные и нестандартные заголовки.

### BUG-062: Купер _friendly_url валит INSERT в Supabase
Файл: parsers/kuper.py
Было: parse() добавлял служебное поле _friendly_url в словарь вакансии для передачи slug-а в enrich(). Поле не удалялось и попадало в INSERT, вызывая APIError PGRST204.
Стало: friendly_url хранится в self._friendly_urls (dict), словарь вакансии не содержит служебных полей.
Причина: контракт BaseParser не предусматривает передачу данных между parse() и enrich() — парсер использовал словарь вакансии как транспорт, что нарушило схему БД.

### BUG-063: Циан API блокирует curl_cffi с impersonate
Файл: parsers/cian.py
Было: POST к api.cian.ru через curl_cffi с impersonate="chrome131" — API возвращал text/html (challenge-страницу) вместо JSON.
Стало: POST через aiohttp session с cookies в заголовке Cookie — API возвращает JSON.
Причина: API api.cian.ru не требует TLS impersonate; curl_cffi с impersonate триггерил другую ветку WAF, которая блокировала запрос.

### BUG-064: общий Playwright-контекст загрязнял cookies между сайтами
Файл: parsers/browser.py
Было: один Playwright-контекст использовался для всех задач; cookies Циана загрязнялись cookies от СберЗдоровья и трекеров; на GitHub Actions API возвращал капчу.
Стало: каждая задача выполняется в изолированном контексте (`browser.new_context` + `context.close`); cookie jar чистый для каждого сайта.
Причина: общий контекст после визита на другой сайт создавал нехарактерный для реального браузера набор cookies, который антибот Циана отклонял.

### BUG-065: sec-ch-ua не совпадал с User-Agent Циана
Файл: parsers/cian.py
Было: `sec-ch-ua` был захардкожен как Chrome/131 при User-Agent Chrome/146 из config.py.
Стало: `sec-ch-ua` обновлён до Chrome/146, консистентно с User-Agent.
Причина: рассинхрон версий в заголовках мог вызывать подозрение у антибота.

### BUG-066: браузерный этап тратил время на cian.ru при выключенном парсере
Файл: parsers/browser.py, main.py
Было: fetch_browser_secrets() открывал cian.ru и собирал cookies, хотя парсер cian отключён (is_enabled=false); main.py логировал cian_cookies=None.
Стало: задача cian_cookies убрана из browser.py, ключ убран из expected_browser_secrets_keys в main.py.
Причина: фирменный парсер Циан заблокирован WAF, cookies не используются ни одним активным парсером; лишний запуск замедлял браузерный этап на 10+ секунд и создавал шум в логах.

### BUG-067: зоопарк title-фильтров расходился между компаниями и ломал расширение HH-парсеров
Файл: config.py, main.py, parsers/hh.py, parsers/hh_cian.py, parsers/hh_zvuk.py, parsers/hh_kaspersky.py
Было: фильтрация зависела от набора per-company whitelist (SBER, DOMCLICK, HH, KASPERSKY), `TITLE_STOP_PATTERNS`, а также служебных полей `is_product_role` и `_source` из HH-парсеров.
Стало: per-company whitelist, `is_product_role` и `_source` удалены; фильтрация выполняется единым `TITLE_WHITELIST_PATTERNS`, blacklist используется только для диагностики логов (`TITLE_BLACKLIST_PATTERNS`), добавлен grade override из заголовка для `младший/junior/head/chief/cpo/директор`.
Причина: разношёрстная логика давала непредсказуемый результат при масштабировании источников и мешала переходу к единой трёхзонной схеме перед AI-фазой.

### BUG-067: MTS-парсер падал на удалённом TITLE_STOP_PATTERNS
Файл: parsers/mts.py
Было: _is_relevant_vacancy() ссылался на config.TITLE_STOP_PATTERNS, который был удалён в рамках Stream 3A (трёхзонная фильтрация).
Стало: проверка по TITLE_STOP_PATTERNS удалена из MTS-парсера. Фильтрация выполняется единым блоком в main.py через TITLE_WHITELIST_PATTERNS. Локальный STRICT_WHITELIST и PM_CATEGORY оставлены как серверный pre-filter.
Причина: при удалении TITLE_STOP_PATTERNS из config.py не был проверен MTS-парсер, который напрямую использовал эту константу.

### BUG-067: regex-паттерны пропускали мусорные вакансии, blacklist не применялся к whitelist
Файл: config.py, main.py
Было: единый TITLE_WHITELIST_PATTERNS с булевым флагом is_regex, blacklist только диагностический, не фильтрующий. Вакансии типа "Менеджер проектов (сопровождение цифровых продуктов)" проходили через regex менеджер.{0,40}продукт.
Стало: три константы (TITLE_EXACT_WHITELIST, TITLE_REGEX_PATTERNS, TITLE_GREY_PATTERNS). Exact — blacklist не применяется. Regex и grey — blacklist применяется после матча. Функция _is_product_title заменена на _classify_title.
Причина: regex с произвольным количеством символов между ключевыми словами ловил вакансии, где "продукт" относился к банковскому продукту или был в скобках описания команды, а не к должности.

### BUG-068: стажёрские вакансии проходили через exact whitelist
Файл: config.py, main.py
Было: exact-зона не применяла blacklist, вакансии типа "Стажёр-менеджер по продукту" проходили через подстроку "менеджер по продукт".
Стало: добавлен TITLE_PREFILTER_REJECT с абсолютным отсечением до зон. "стажер", "стажёр", "intern" удалены из TITLE_BLACKLIST_PATTERNS.
Причина: стажёрские вакансии не являются целевыми для продукта, но содержат точные PM-подстроки в заголовке.

### BUG-069: широкие exact-подстроки пропускали нецелевые вакансии
Файл: config.py
Было: TITLE_EXACT_WHITELIST содержал усечённые подстроки "менеджер продукт", "менеджер по продукт", "владелец продукт", которые матчили нецелевые вакансии (например "менеджер по продуктовому маркетингу"). Exact-зона не проверяет blacklist, поэтому мусор проходил напрямую.
Стало: широкие подстроки заменены на точные падежные формы. Нестандартные формулировки проваливаются в regex-зону и проверяются blacklist. Добавлены "поддержк" и "сопровожден" в TITLE_BLACKLIST_PATTERNS.
Причина: усечённые основы слов покрывали слишком много вариантов, включая нецелевые роли.

### BUG-067: parser_name не уникален для компаний одного парсера
Файл: bot/onboarding.py, bot/handlers.py, delivery/telegram.py
Было: callback_data и mute-команды использовали parser_name. У компаний МТС и X5 (один парсер — несколько компаний) callback_data совпадал, toggle переключал все кнопки разом, mute блокировал не ту компанию.
Стало: используется slug — уникальный идентификатор компании из таблицы companies.
Причина: parser_name — идентификатор парсера, не компании. При паттерне один-парсер-несколько-компаний он не уникален.

### BUG-068: toggle компании в settings не сохранялся между страницами
Файл: bot/handlers.py
Было: toggle менял только markup текущего сообщения без записи в БД. При переключении страницы или нажатии Save учитывались только кнопки текущей страницы.
Стало: каждый toggle сразу записывает изменение в excluded_companies через db.update_user_filters. Страницы всегда рисуются из актуальных filters.
Причина: паттерн «накопить в markup, сохранить при Save» не работает для пагинированного списка — при смене страницы markup уничтожается.

### BUG-069: toggle компании в /settings пересортировывает список
Файл: bot/handlers.py
Было: при toggle вызывался get_company_page, который пересортировывал blocked/active — компания мгновенно перемещалась между секциями.
Стало: при toggle вызывается toggle_selection, которая меняет только emoji кнопки без пересортировки. Порядок фиксируется при входе в список и при навигации по страницам.
Причина: get_company_page каждый раз заново разбивает и сортирует список, что противоречит UX-решению о фиксации порядка внутри сессии.

### BUG-070: сортировка компаний по Unicode вместо русского алфавита
Файл: bot/onboarding.py
Было: get_company_page сортировал по name.lower(), давая порядок: цифры, латиница, кириллица (2ГИС, KION, Авиасейлс).
Стало: сортировка по sort_name из таблицы companies — русская транслитерация названия (Lamoda -> Ламода, KION -> КИОН), порядок соответствует ожиданиям русскоязычного пользователя.
Причина: Python сортирует строки по Unicode codepoints, где латиница (U+0041) идёт раньше кириллицы (U+0410).

### BUG-071: scheduled-рассылка называла старые вакансии новыми
Файл: main.py
Было: текст «Новые вакансии по твоим фильтрам» отправлялся для всех недоставленных вакансий, включая вакансии из прошлых выпусков.
Стало: выдача разделяется на «новые» (нет записи в delivery) и «ранее анонсированные» (`status=announced`), вводный текст стал честным.
Причина: бинарная модель доставки не различала «пользователь не знает о вакансии» и «пользователь знает, но не открыл карточку».

### BUG-072: кнопка Хватит не сохраняла информацию об оставшихся вакансиях
Файл: bot/handlers.py
Было: при нажатии «Хватит» оставшиеся вакансии сохранялись как «не доставлены» и возвращались в следующей рассылке как новые.
Стало: оставшиеся вакансии помечаются как `announced`, в будущих рассылках не считаются новыми.
Причина: отсутствие промежуточного статуса между «не видел» и «получил карточку».

### BUG-073: _build_more_keyboard возвращал невалидную inline_keyboard-структуру
Файл: bot/handlers.py
Было: inline_keyboard собирался через конкатенацию списков, и кнопка «Хватит» попадала вложенным списком в ряд кнопок.
Стало: keyboard разделён на два отдельных ряда: (1) «Ещё 10/Показать оставшиеся N» + «Все (N)», (2) «Хватит».
Причина: Telegram API ожидает массив рядов, где каждый ряд — список кнопок-словарей без дополнительной вложенности.

### BUG-067: ReplyKeyboardMarkup передавался в editMessageText в callback-сценариях
Файл: bot/handlers.py
Было: в ветках mute/unmute/unmute_all, more:stop и st:close использовался `edit_message(..., reply_markup=build_main_reply_keyboard())`, что нарушает контракт Telegram API для `editMessageText`.
Стало: в этих сценариях выполняется `delete_message` (в try/except), затем отправляется одно `send_message` с тем же текстом; для mute/unmute/unmute_all inline-действие объединено в это же сообщение.
Причина: ReplyKeyboardMarkup нельзя передавать в editMessageText, а главное reply-меню уже закреплено у пользователя после онбординга и не требует повторной отправки.

### BUG-070: grade_priority не учитывал грейд пользователя
Файлы: bot/handlers.py, main.py
Было: _grade_priority возвращала фиксированный приоритет грейда вакансии без учёта пользовательских предпочтений; scheduled delivery вообще не учитывал grade при сортировке.
Стало: grade_score вычисляет релевантность на основе расстояния до пользовательских грейдов; rank_vacancies используется как единая точка сортировки в on-demand и scheduled.
Причина: фиксированная шкала ставила Lead+ выше всех для любого пользователя, даже если он выбрал Junior/Middle.

### BUG-074: Незащищённые вызовы _send_onboarding_batch оставляли вечный лоадер при ошибке DB
Файл: bot/handlers.py
Было: в ветках `ob:quick`, `ob:strict:off`, `ob:strict:on`, `hub:vacancies`, `st:deliver` вызов `_send_onboarding_batch` не был обёрнут в `try/except`; при исключении после установки лоадера сообщение зависало с текстом «⏳ Подбираю вакансии...». 
Стало: все пять вызовов обёрнуты в `try/except` с `logging.exception` и fallback через `edit_message`.
Причина: ошибка БД внутри `_send_onboarding_batch` не перехватывалась в обработчике callback.

### BUG-075: Переход ob:next оставлял лоадер при сбое _handle_step_transition
Файл: bot/handlers.py
Было: вызов `_handle_step_transition` в ветке `ob:next` выполнялся без `try/except`; при ошибке после лоадера «⏳ Применяю настройки...» пользователь оставался без финального сообщения.
Стало: вызов `_handle_step_transition` обёрнут в `try/except` с `logging.exception` и fallback через `edit_message`.
Причина: исключение из DB-операций перехода шага не обрабатывалось на уровне callback-ветки.

### BUG-076: st:edit:company оставлял лоадер при падении get_enabled_companies
Файл: bot/handlers.py
Было: в ветке `st:edit:company` лоадер «⏳ Загружаю список компаний...» ставился до `db.get_enabled_companies()` без защиты.
Стало: блок лоадера и `db.get_enabled_companies()` обёрнут в `try/except` с `logging.exception` и fallback через `edit_message`.
Причина: ошибка БД прерывала сценарий после скрытия кнопок, и пользователь оставался на зависшем экране.

### BUG-077: ReplyKeyboardRemove в edit_message вызывал HTTP 400 в st:stop:yes
Файл: bot/handlers.py
Было: ветка `st:stop:yes` вызывала `edit_message(..., reply_markup=build_reply_keyboard_remove())`, что невалидно для `editMessageText` и приводило к HTTP 400.
Стало: `edit_message` вызывается с `reply_markup=None`, а удаление постоянной клавиатуры вынесено в отдельный `send_message(..., reply_markup=build_reply_keyboard_remove())`.
Причина: Telegram API принимает в `editMessageText` только inline-разметку, а `ReplyKeyboardRemove` допустим только в `sendMessage`.
